- [x] Fix dropdown + logout so they work on both /dashboard and /admin
  - [x] Update public/admin.html to include the same topbar dropdown + logout form + session/role gating script used by public/dashboard.html
  - [x] Ensure dropdown toggles and closes on outside click / Escape
  - [x] Ensure logout submits POST /logout and redirects to /login
- [ ] (After fixes) Run server and manually verify dropdown/logout on:
  - [ ] /dashboard
  - [ ] /admin


